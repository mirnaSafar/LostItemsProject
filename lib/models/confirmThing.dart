import 'package:flutter/material.dart';

class confirmThing {
  final String confirmImage;
  final String confirmMoney;
  final String confirmDescription;

  confirmThing({this.confirmImage, this.confirmMoney, this.confirmDescription});
}
